Primary Features :
1. หน้าจอลงทะเบียน ประกอบด้วย
  - เพิ่มรูป Bandner ตามรูปข้างล่างนี้นะ
  - เลือกสายงาน
  - เลือกสำนัก (กรองข้อมูลตามสายงาน)
  - เลือกชื่อ-นามสกุล (กรองข้อมูลตามสำนัก)
  - ป้อนเบอร์โทรโต๊ะ
  - sytem date วันที่และเวลาลงทะเบียน (แต่ไม่ต้องแสดงตรง frontend ให้แสดงตรง Backend)
  - มีปุ่มให้คลิกลงทะเบียน เมื่อคลิกให้แล้วขึ้น Popup ขอบคุณ ลงทะเบียนสำเร็จ (ดูจากรูปด้านล่าง)

***หน้าจอลงทะเบียน เจเจ อาจจะเอาจาก one nbtc ก็ได้นะครับ แล้วมาปรับเอา Filed ที่ไม่จำเป็นออกอีกที เพราะคล้ายๆกัน

2. สามารถออกรายงานเป็น Excel สรุปผู้มาลงทะเบียนตามรูปแบบ Excel ข้างล่างนี้ครับ

3. มีหลังบ้านสามารถแก้ไขข้อมูลได้ตามนี้
  -ข้อมูลพนักงาน เช่น ชื่อ-นามสกุล ตำแหน่ง สำนัก สายงาน
  -ข้อมูลการลงทะเบียน กรณีเขาลงผิด สามารถลบ ได้
  -สามารถ import ข้อมูลพนักงาน อัพเดตได้

ถ้าติดปัญหาอะไร แจ้งพี่อีกทีนะครับ...


frontend มีหน้าลงทะเบียน หน้าจอเดียวครับ
backend มีหน้าจอตามรายละเอียดข้างบนครับ


Today


Tomorrow



[Done]
- do a logging system for access monitoring
- name search type input public
- add excel import button that specifically list the non-existing name employees (set is_delete to 1) in case they quit or retire
 -*- for easier finding later and safely manually delete
- Make new backend admin page to manage users, without relying on command lines
- increase session security for sending to backend (use a fulyl manually coded jwt-like token that I should invent my own gibberish)
 -*- except for creating register, the only POST method that should be spared of security lock
 -*- add sesssion timeout logic when inactive for 1 hour
- add username field back again in users table and use it as primary option of logging in
- set up database for permission
- prevent 502 bad gateway memory leak overnight
- ssl https secure connection
- deploy AD login into it
- put deploy test server (this week)
- export group by : order by : dept and div (for orderly read for users)
- import excel large problem (maybe to csv)
- duplicate register is allowed, but for same name, only allow recent entry for each employee name for excel export
- submit for the real test
- Test cases (users)
 - Blind accessibility in form page
   - banner got "alt" to read
   - modal's auto focus for easy tab
- Test cases (admin)
 - Import excel for updating employees
 1. **File validation**
   - Non-Excel file upload (.txt, .pdf, .jpg)
   - Corrupted Excel file
   - Empty Excel file
   - Very large Excel file (>50MB)
2. **Excel data scenarios**
   - Missing columns in Excel
   - Extra columns in Excel
   - Different column order
   - Mixed data types in columns
   - Special characters in names
   - Unicode/Thai characters
3. **Data matching failures**
   - Division name misspelled
   - Department not in selected division
   - Position name variations (with spaces, abbreviations)
   - Duplicate employee names
4. **Import confirmation flow**
   - Cancel during test import
   - Cancel during real import
   - Navigate away during import
   - Browser refresh during import
1. **Export scenarios**
   - Export with 0 registrations
   - Export with 1000+ registrations
   - Export while data is changing
- Make Back end pages for admin only
  -ข้อมูลพนักงาน เช่น ชื่อ-นามสกุล ตำแหน่ง สำนัก สายงาน
  -ข้อมูลการลงทะเบียน กรณีเขาลงผิด สามารถลบ ได้
  -สามารถ import ข้อมูลพนักงาน อัพเดตได้
  -export excel report button
- Beautify Font in front end
- Solidify front end pages
- Design and solidify database structure
  (pair thai name and english name for front-back balance)
  "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysqldump.exe" --no-defaults -u root -p -h localhost --port=3307 --no-data onenbtc > template.sql
  pwd : rootsql

  "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysqldump.exe" --no-defaults -u root -prootsql -h localhost --port=3307 onenbtc > complete_backup.sql















  Ubuntu Server
Minimum Requirements:
- OS: Ubuntu 22.04 LTS or 20.04 LTS
- RAM: 4 GB (8 GB recommended for development)
- Storage: 20 GB SSD (40 GB recommended)
- CPU: 2 cores (4 cores recommended)
- Network: Bridged adapter for local access

Production Requirements:
- RAM: 8 GB+
- Storage: 50 GB+
- CPU: 4 cores+
- Backup storage for database


Project Structure:          Estimated Size:
- Node.js modules           ~300 MB
- React build               ~50-100 MB
- MySQL data                ~1-5 GB (grows with usage)
- Logs                      ~2-5 GB (monthly)
- Backups                   ~2-10 GB
----------------------------
Total Minimum:             ~3.5 GB (fresh)
Recommended Buffer:        +20 GB for growth
---------------------------
✅ Your 50 GB spec is PERFECT

Recommended Upgrade:
- RAM: 8 GB (instead of 4 GB)
  Reason: MySQL 8.0 + Node.js + Chrome/Firefox + IDE
- Storage: 40 GB SSD
  Reason: Snapshots + Docker images + node_modules
- CPU: 4 cores (your recommendation is perfect)

Production
Your Spec is PERFECT:
- RAM: 8 GB (consider 16 GB if >100 concurrent users)
- Storage: 50 GB + external backup (excellent!)
- CPU: 4 cores (ideal for workload distribution)

